/// <reference path="../typings/mocha/mocha.d.ts"/>

var app = require('./helpers/app');

var should = require('should'),
	supertest = require('supertest');
	
describe('flights', function name() {
	
	it('should pass', function name(done) {
		done();
	});
	
	it('should not pass', function name(done) {
		//throw "don't pass";
		done();
	});
	
});